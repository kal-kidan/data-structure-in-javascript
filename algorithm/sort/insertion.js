function sort(a) {
    let b = [a[0]];
    temp;
    for (let k = 1; k < a.length; k++) {
        insert(a[k]);

    }
    return a;

    function insert(item) {
        for (let i = k + 1; i < a.length; i++) {}
    }
}

console.log(sort([23, 4, 7, 1, 39]));